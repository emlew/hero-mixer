import styled from "@emotion/styled";
import { Box } from "@mui/material";

export const StyledPlayers = styled(Box)({
  display: "flex",
  flexDirection: "row",
  gap: "15vw",
});

export const StyledSetup = styled(Box)({
  display: "flex",
  flexDirection: "column",
  alignItems: "center",
});
