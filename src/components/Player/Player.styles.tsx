import { Box, styled } from "@mui/material";

export const StyledPlayerDetails = styled(Box)({
  display: "flex",
  flexDirection: "row",
  alignItems: "center",
  height: "60px",
  padding: "5vh 5vw",
});
