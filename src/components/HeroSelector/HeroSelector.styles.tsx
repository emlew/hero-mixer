import styled from "@emotion/styled";
import { Box, FormControl } from "@mui/material";

export const StyledFormControl = styled(FormControl)({
  width: "10vw",
});

export const StyledHeroDetails = styled(Box)({
  display: "flex",
  flexDirection: "column",
  width: "10vw",
});
